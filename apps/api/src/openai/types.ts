/**
 * OpenAI-Compatible API Types
 *
 * Kept as a local re-export so the API package can import from `./types`.
 */

export type {
  OpenAIChatRequest,
  OpenAIChatResponse,
  OpenAIImageRequest,
  OpenAIImageResponse,
  OpenAIModelsListResponse,
} from '@z-image/shared'
